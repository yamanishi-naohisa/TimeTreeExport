# TimeTreeExport 仕様書

## プロジェクト概要

TimeTreeから今日以降のスケジュールを取得し、CSV形式でエクスポートするPythonスクリプト

## 機能要件

### 1. TimeTreeへのアクセス
- TimeTreeのカレンダーデータにアクセスする
- 認証情報（APIキー、トークンなど）を使用して認証を行う
- **注意**: TimeTreeの公式APIは2023年12月22日で終了しているため、以下の代替手段を検討：
  - 非公式APIの利用（可能な場合）
  - カレンダー同期機能（Googleカレンダーなど）経由での取得
  - エクスポート機能の利用
  - スクレイピング（利用規約に準拠する場合）

### 2. スケジュールデータの取得
- **対象期間**: 今日以降のスケジュール
- **取得する情報**:
  - イベント名（タイトル）
  - 開始日時
  - 終了日時
  - 場所
  - 説明（メモ）
  - カレンダー名
  - 参加者（可能な場合）
  - 繰り返し設定（可能な場合）

### 3. データのフィルタリング
- 今日以降のイベントのみを抽出
- 必要に応じて、特定のカレンダーのみを対象とする

### 4. CSV形式への変換
- 取得したスケジュールデータをCSV形式に整形
- **CSVの列構成**:
  - 日付（YYYY-MM-DD）
  - 開始時刻（HH:MM）
  - 終了時刻（HH:MM）
  - タイトル
  - 場所
  - 説明
  - カレンダー名

### 5. CSVファイルの出力
- デフォルトファイル名: `timetree_export_YYYYMMDD_HHMMSS.csv`
- 出力先: スクリプト実行ディレクトリまたは指定ディレクトリ
- 文字コード: UTF-8（BOM付き、Excel互換性のため）

## 非機能要件

### エラーハンドリング
- APIアクセスエラー時の適切なエラーメッセージ表示
- ネットワークエラー時のリトライ機能（オプション）
- データ取得失敗時のログ出力

### 設定管理
- 認証情報は環境変数または設定ファイルで管理
- `.env`ファイルを使用（`.gitignore`に含める）

### ログ出力
- 実行ログを出力（オプション）
- デバッグモードのサポート

## 技術スタック

- Python 3.8以上
- 必要なライブラリ:
  - `requests` - HTTPリクエスト用
  - `python-dotenv` - 環境変数管理
  - `csv` - CSV出力（標準ライブラリ）
  - `datetime` - 日時処理（標準ライブラリ）

## 実装方針

1. **モジュール構成**:
   - `main.py` - エントリーポイント
   - `timetree_api.py` - TimeTree APIアクセス処理
   - `csv_exporter.py` - CSV出力処理
   - `config.py` - 設定管理
   - `utils.py` - ユーティリティ関数

2. **設定ファイル**:
   - `.env` - 認証情報（gitignore対象）
   - `.env.example` - 設定例（Git管理対象）
   - `config.py` - デフォルト設定

## 今後の検討事項

- [ ] TimeTreeへのアクセス方法の確定（API/同期/エクスポート）
- [ ] 認証方式の詳細
- [ ] エラーハンドリングの詳細仕様
- [ ] ログ出力の詳細仕様
- [ ] テスト方針

